
export const host = "localhost";
export const port = 3912;

export const applicationPath = "explorer";
export const dataPath = "radata";

/** 
 * Updates will be published with at least this delay between them, to not overwhelm the CPU and user.
 * In milliseconds.
 */
export const minUpdateDelay = 3000;
